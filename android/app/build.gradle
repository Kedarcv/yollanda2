android {
    defaultConfig {
        applicationId "com.yourcompany.gammalabs" // Replace with your app's unique ID
        ...
    }
    signingConfigs {
        release {
            storeFile file('keystore.jks')
            storePassword 'Cvlised@360'
            keyAlias 'your-key-alias'
            keyPassword 'Cvlised@360'
        }
    }
    buildTypes {
        release {
            signingConfig signingConfigs.release
        }
    }
}
